import './Header.css';

function Header() {
    return (
        <header className="header">
            <img src="/img/Header.png" alt="header"/> 
        </header>
        
    )
}

export default Header //permite que se pueda usar el componente y que se visualice